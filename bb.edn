{:paths ["src" "test"]
 :tasks
 {test {:doc "Run the standalone actor test suite"
        :task (shell "kbb run_tests.cljk")}
  analyze {:doc "Generate canonical EDN comparison artifacts"
           :task (shell "kbb -m hydrogen-electrolysis.methods.analyze")}
  deploy-plan {:doc "Render the operator-gated ingest plan without network writes"
               :task (shell "kbb -m hydrogen-electrolysis.kotoba.deploy")}}}
